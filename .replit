run = "uvicorn server.main:app --host 0.0.0.0 --port 8080"
entrypoint = "server/main.py"
modules = ["python-3.11"]

[deployment]
run = ["sh", "-c", "uvicorn server.main:app --host 0.0.0.0 --port 8080"]
deploymentTarget = "cloudrun"

[[ports]]
localPort = 8080
externalPort = 80
